<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container">
        <%= link_to 'Message Me', root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to 'Chatroom', root_path, class: 'nav-link active' %>
                </li>
            </ul>
            <div class="d-flex gap-3 align-items-center">
               <% if logged_in? %>
                <%= link_to 'Logout', logout_path, class: 'btn btn-outline-primary', method: :delete %>
               <% else %>
                <%= link_to 'Login', login_path, class: 'btn btn-primary-gradient' %>
               <% end %>
            </div>
        </div>
    </div>
</nav>
