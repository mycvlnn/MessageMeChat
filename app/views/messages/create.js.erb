// Clear the message input after successful send
document.querySelector('.message-input').value = '';

// The message will be broadcast via ActionCable automatically
// No need to append here, ActionCable will handle it
